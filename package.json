{
  "scripts": {
    "serve": "live-server .",
    "watch:style": "sass style.scss style.css --watch",
    "build:style": "sass style.scss style.css --style compressed",
    "build": "npm run build:style"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "sass": "^1.55.0"
  }
}
